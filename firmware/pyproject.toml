[project]
name = "visio-recorder"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "supabase>=2.4.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0.0"]

[tool.setuptools]
packages = ["visio_recorder"]

[tool.pytest.ini_options]
testpaths = ["tests"]
